## 1. CLI 语义统一与兼容层（P0）

- [x] 1.1 梳理现有参数并定义统一语义（页面规格、输出格式、路径参数），形成映射清单
- [x] 1.2 在 `src/bin/mark2pdf.ts` 与 `src/cli/handler.ts` 增加集中参数映射逻辑，统一进入 `convert` 执行路径
- [x] 1.3 为历史参数接入弃用提示文案（含替代参数建议），并保持命令可继续执行
- [x] 1.4 更新 CLI `--help` 文案，确保参数语义与实际行为一致

## 2. 统一入口与 dry-run 计划执行（P0）

- [x] 2.1 在 `convert` 路径实现输入类型识别与最小分发（复用现有处理器）
- [x] 2.2 抽取统一“执行计划生成”逻辑，输出待处理文件、目标路径、跳过原因
- [x] 2.3 接入 `--dry-run`，确保仅输出计划且不创建文件、不触发实际转换
- [x] 2.4 校验 dry-run 与真实执行在文件选择和路径推导上的一致性

## 3. 配置可观测能力（P1）

- [x] 3.1 在 `src/config/loader.ts` 增加关键配置项来源标注（default/user/file/env/cli）
- [x] 3.2 提供配置可见输出入口（如 `--show-config` 或 verbose 增强输出）
- [x] 3.3 确保展示的最终配置与实际执行使用配置一致
- [x] 3.4 过滤敏感信息输出，避免泄露环境变量原值

## 4. 错误可操作与结构化报告（P1）

- [x] 4.1 统一 CLI 错误输出协议（错误类别、问题摘要、建议动作）
- [x] 4.2 统一 `convert/html/merge/extract` 的错误呈现风格与关键字段
- [x] 4.3 增加结构化执行报告（JSON）输出能力，包含总数/成功/失败/耗时/失败明细
- [x] 4.4 保证终端摘要与 JSON 报告统计一致

## 5. 文档与验证

- [x] 5.1 更新 README 示例，覆盖统一入口、dry-run、配置可观测、报告输出
- [x] 5.2 补齐/重构测试：参数兼容、统一分发、dry-run 无副作用、配置来源可见、错误输出与报告一致性
- [x] 5.3 运行测试并修复阻塞项，确认可进入实现阶段
