## 1. CLI 参数语义统一

- [ ] 1.1 梳理现有 `convert` 相关参数含义与冲突点，定义“输出格式/页面规格”最终命名与映射规则
- [ ] 1.2 在参数解析层实现新旧参数映射与统一入口（保留旧参数兼容）
- [ ] 1.3 为旧参数增加弃用提示文案，明确替代参数

## 2. convert 统一入口最小调度

- [ ] 2.1 在 `convert` 命令路径中加入输入类型识别与分发逻辑（复用现有处理器）
- [ ] 2.2 统一多输入类型下的错误返回与提示格式，保证行为一致
- [ ] 2.3 为边界输入补充最小校验（不支持格式、路径异常）并给出可操作提示

## 3. dry-run 预检能力

- [ ] 3.1 为转换命令新增 `--dry-run` 选项并接入执行流程
- [ ] 3.2 实现 dry-run 计划输出：待处理文件、输出路径、跳过原因
- [ ] 3.3 确保 dry-run 不产生副作用（不创建文件、不触发转换）
- [ ] 3.4 校验 dry-run 与真实执行在文件选择和路径推导上的一致性

## 4. 文档与回归验证

- [ ] 4.1 更新 CLI `--help` 文案，反映新参数语义与弃用提示
- [ ] 4.2 更新 README 命令示例，确保与实际行为一致
- [ ] 4.3 增加/更新测试：参数兼容、统一入口调度、dry-run 无副作用与输出内容
- [ ] 4.4 执行测试并修复阻塞项，确认本变更可进入实现阶段
